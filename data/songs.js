"use strict";

/* ============================================================
   DATEN — Songs
   Reserviert für eine spätere Phase (Songbegleitung/Bibliothek).
   Enthält aktuell bewusst keine Daten und keine Funktionalität.
   ============================================================ */
const SongsData = {
  // Wird in einer späteren Phase mit Song-Daten befüllt.
};
